# 今天，怎么心动？ · 方向与接 API 的路线

这份文件只回答两个问题：**这个网站要往哪走**，以及**将来接 API 时先接哪个、怎么接**。
样式和文案的规矩在 `DESIGN.md`。

---

## 一、产品的一句话

> 一个人动动手指，就能把一天排好，然后送给另一个人。

所以每次改动只看两个指标：

1. **送出去的成本**——从打开到"链接已复制"要几步？现在是 2 步（一键定今天 → 把今天送给她）。
2. **收到时的心动**——对方点开的第一屏是不是一个惊喜，而不是一个表单。

会让这两件事变难的功能（登录、账号、下载 App、填一堆偏好），无论多"完整"，都先不做。

---

## 二、现在已经能做到的

- 单个 `index.html`，纯静态，GitHub Pages 托管，**没有后端、没有数据库、没有埋点、没有任何 key**。
- **一键定今天**：按当前时间、预算、距离、心情自动抽卡自动选，沿时间轴挑出连着走的三站。
- **真实时间轴**：每张卡带时长和可开始的时段，从"现在 + 25 分钟"往后排，站间留 15 分钟，
  谁都不会被排在自己营业时段之外，也不会跑过次日 01:00。
- **哪一天**：今天 / 明天 / 这周末 + 出发时段。深夜自动改排明天。
- **附近真实的地方**：定位之后直接问 OpenStreetMap（Overpass + Nominatim，都不需要 key，
  都支持 CORS），把周边的餐厅、咖啡馆、公园、美术馆、影院变成卡片，带步行/车程、菜系、营业时间。
- **出逃半日**：先在你选的公里数（30/50/80）附近挑一个镇当目的地，再围着那个镇找吃喝玩乐；
  "开车去某某镇"本身就是第一站，带公里数和车程。
- **偏好**：口味、喜欢的场景、不想碰的词，以及"私藏地点"——写进设置的地方会变成卡片一起抽。
- **送出去**：整份计划 base64 进 `?s=`，她点开先看到盖着你名字的封蜡；还能存成海报图片、复制文字版。
- **打卡**：计划出来之后每一站左边有个圈，到了就点一下。它会记下时间、位置
  （这一站本身有坐标就用它的，没有才问一次定位），并且可以挂一张照片。
- **足迹地图**：打过卡的地方会亮在一张地图上。自己用 canvas 画的极简瓦片地图，
  底图是 OpenStreetMap，**不需要 key、没有引任何地图库**，配色跟着明暗主题变。
  有照片的钉子会把照片直接画在地图上。
- **照片**：调相机拍或从相册选，压好之后存在 IndexedDB 里（localStorage 放不下），
  在计划里、照片条里、地图上三处都能看到。
- **回忆本**：生成过的计划存在本机，可以回看，并显示这一份打了几站。

外部依赖全部是"可选增强"：接口挂了、限流了、拒绝定位，界面和手写卡池照常可用。

### 两个调试用的地址参数

- `?t=20:30` —— 假装现在是晚上八点半，用来预览别的时间点会怎么排
- `?api=https://你的域名` —— 让 `Sources` 走你自己的接口（见下一节）

---

## 三、接 API 的接入点：`Sources`

`index.html` 第 3 节 `Sources` 就是为此留的那一层：

```js
Sources.cards(ctx, cb)          // 卡池：默认本地 POOL
Sources.weather(lat, lon, cb)   // 天气：默认 open-meteo
Sources.sweet(ctx, cb)          // 情话：默认本地 SWEETS
```

三个都是「回调 + 失败静默回落本地」。加 `?api=` 就走远端，不加就完全离线。
第 3b 节的附近地点是同一套思路，只是数据源换成了 Overpass。

### 约定的返回格式

**`GET /cards?lat&lon&hour&budget&distance`**

```json
{ "cards": [
  { "category": "食", "title": "临江那家小馆", "detail": "点一道她没吃过的招牌菜",
    "weather": "皆", "distance": ["🌆 市区逛逛"], "budget": 1,
    "window": [11, 19], "minutes": 80,
    "meta": "步行 8 分钟 · 川菜", "rating": 4.6, "reviews": 1280,
    "hours": "营业到 21:30", "summary": "评价里最常提到的是水煮鱼和靠窗的位置",
    "near": true, "source": "amap:B0FF..." }
]}
```

| 字段 | 含义 | 缺省 |
|---|---|---|
| `category` | `行` / `游` / `食` / `彩蛋` | `游` |
| `weather` | `皆` / `晴` / `雨` | `皆` |
| `distance` | 允许的距离档，数组 | 市区 |
| `budget` | 0 穷开心 / 1 小奢侈 / 2 豪华版 | 0 |
| `window` | `[最早开始, 最晚开始]`（整点小时） | 按类型给默认值 |
| `minutes` | 大概要多久，排时间轴用 | 玩 90 / 吃 75 / 彩蛋 30 |
| `meta` `rating` `reviews` `hours` `summary` | 会被拼成卡片上那行灰字 | 无则不显示 |

远端卡片**追加**在本地卡池后面一起抽，不会把手写文案挤掉——文案是这个网站的命，不交给接口。
`detail` 那一行永远是我们的话，事实只放在 `meta` 系列字段里。

**`GET /weather?lat&lon`** → `{ "temp": 26, "code": 3 }`（WMO weather code，和 open-meteo 一致）

**`GET /sweet?hour`** → `{ "line": "……" }`

---

## 四、按优先级排的三步

### 第一步：口碑与评价 —— 前端和后台都写好了，只差部署

**情侣评分已经做完了**，而且是按情侣的标准来的，不是抄五星：

- 总体几颗心，加五个维度（能安静说话 / 位置适合两个人 / 可以久坐 / 拍照好看 / 不用排队）、
  适合的场合（第一次约会 / 纪念日 / 随便逛逛 / 深夜 / 带家人也行）、花费档，和一句话
- 打完卡就在那一站下面评分，评过的地点在卡片上会多一行
  `情侣评分 4.4（42 对）· 和你们像的 4.7 · 位置适合两个人`
- 这个分会**反过来影响抽卡**：分高的地点更容易被抽到
- **MBTI**：设置里填你们俩的类型，抽卡会往你们这一型偏，
  并且后台会单独算一份「和你们 MBTI 接近的情侣」的分数

**后台也写好了，在 `server/`**：Cloudflare Worker + D1，一条 `wrangler deploy` 就能上线，
带 schema、限流、去重、下架开关和隐私说明。部署完用 `?api=https://…` 打开网站就接上了。
在这之前评分不会丢——先存在本机排进 outbox，后台一上线自动补交。

`server/README.md` 里写了 MBTI 相似度是怎么算的：不能把一对情侣压成"共识字母"
（两人一致取该字母、不一致记 `?`），否则互补型的情侣（ENFP + ISFJ 四个轴里三个相反）
会永远匹配不到任何人，连另一对同样组合的都匹配不上。所以按"这一对里有几个人偏这一头"
算成 0 / .5 / 1，再比两对之间的差。

### 还差的：真实的口碑数据

情侣评分要攒到有意义的量，需要人。在那之前，地点的口碑还得靠现成的数据源。
OSM 已经把"附近有哪些地方"解决了，但它**没有评价、没有人均、没有照片**，而且在国内：

- 只有大约 **6%** 的店填了 `opening_hours`，营业时间大多要靠类型默认值兜
- 有些地方只有英文名（代码里已优先取 `name:zh`）
- 连锁店会重复（已按名字去重留最近的一家）

要做到"根据各种 review 做一个总结"，必须换数据源：

- **国内：** 高德地图 Web 服务（周边搜索 + 营业时间）配大众点评/美团（评分、人均、评价）。
- **海外：** Google Places（`rating`、`user_ratings_total`、`reviews`、`opening_hours` 一次给全）。
- **必须有一层自己的代理。** key 不能放进静态页面，会被人拿去刷额度。最省事的是一个
  Cloudflare Worker / Vercel Function，做四件事：藏 key、按经纬度网格缓存、把返回裁成上面那个
  `cards` 格式、把几条评价交给小模型压成一句 `summary`（"评价里最常提到的是水煮鱼和靠窗的位置"）。
- 这一步做完，卡片上那行灰字就会从"步行 8 分钟"变成
  "步行 8 分钟 · 川菜 · 4.6 分（1280 条）· 营业到 21:30 · 评价里最常提到靠窗的位置"，
  而**前端一行都不用改**——`normCards` 已经在等这些字段了。

### 第二步：短链 + 链接预览图 + 「她拆开了」

现在的分享链接有 800～1200 个字符，能用，但发到聊天窗口里很长，也做不了链接预览。

- `POST /plans` 存一份计划 → 返回 6 位 id；`GET /p/:id` 取回。链接变成 `…/?p=aB3x9Q`。
- 有了服务端就能给链接配 `og:image`（一张计划封面，海报那套 canvas 逻辑挪过去就行），
  微信/iMessage 里会显示成一张卡片，点开率完全不同。
- 顺手能做**「她拆开了」**：`GET /p/:id` 第一次被打开时给发起人推一条提醒。
  这是这类产品里最打动人的一个功能，成本却很低。
- 一旦开始存计划就有了数据责任。建议**不要账号**，只存计划本身 + 30 天过期，链接即凭证。
- **顺带解决足迹地图最大的遗憾**：现在打的勾和照片都只存在打卡那台手机上，
  两个人各有一张自己的地图。要真正做到"两个人一起点亮同一张地图"，
  必须有一处共同的存储——所以这一步的价值不只是短链，而是这两张地图能合成一张。
  实现上仍然可以不要账号：一对情侣共用一个不可枚举的 `couple id`（存在设置里、
  跟着分享链接走），打卡和照片都挂在这个 id 下。照片建议只传缩略图，原图留在本机。

### 第三步：让文案会写字

- 用小模型根据「天气 / 时间 / 已选的几站 / 称呼 / 私藏地点」现写落款和情话，
  替换固定的 `ENDINGS` 和 `SWEETS`。
- 更值钱的用法不是情话，而是**给每一站写一句只属于他们的备注**
  （"上次你说想吃这家的糖水，今天补上"）。这需要历史数据（回忆本），所以排在短链之后。
- 同样走自己的代理，设每日额度上限，失败一定回落到本地文案。

---

## 五、明确不做的方向

- **登录 / 账号体系**：会把 2 步变成 6 步。回忆本用 `localStorage`，跨设备用"设置链接"搬过去。
- **社交流 / 广场 / 别人的约会计划**：这是一个两个人的东西，第三个人一进来就不心动了。
- **打包成 App**：分享路径是链接，装 App 只会让链接更难点开。
- **付费墙**：如果真要收钱，收的应该是"帮你订好"（代订、日历导出），不是"解锁更多卡片"。

---

## 六、维护须知

- 全站就一个 `index.html`，JS 按编号小节排好（1 小工具 … 12 开场/设置/彩蛋/情话），改动前先看小节标题。
- 有意只用 ES5 语法（`var` / `function` / 回调），因为很多人是在微信内置浏览器里打开的。
- 样式一律走 `:root` 里的令牌，不要在组件里写死数值，规矩见 `DESIGN.md`。
- 兼容性别丢：旧的 `?c=` 设置链接必须一直能打开，已经发出去的链接不能失效。
- `favicon`、连点 5 次页脚的彩蛋、`4-15` 和 `12-6` 的自动彩蛋都是有意保留的，别顺手删掉。
- 时间相关的改动记得跑一遍多时段自测：不同时刻下每一站都必须落在自己的营业时段内。
