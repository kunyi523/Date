# 今天，怎么心动？ · 方向与接 API 的路线

这份文件只回答两个问题：**这个网站要往哪走**，以及**将来接 API 时先接哪个、怎么接**。

---

## 一、产品的一句话

> 一个人动动手指，就能把"今天"排好，然后送给另一个人。

所以每次改动只看两个指标：

1. **送出去的成本**——从打开到"链接已复制"要几步？现在是 2 步（一键定今天 → 把今天送给她）。
2. **收到时的心动**——对方点开的第一屏是不是一个惊喜，而不是一个表单。

会让这两件事变难的功能（登录、账号、下载 App、填一堆偏好），无论多"完整"，都先不做。

---

## 二、现在是什么形态

- 单个 `index.html`，纯静态，GitHub Pages 直接托管，**没有后端、没有数据库、没有埋点**。
- 设置和回忆本存在浏览器 `localStorage` 里，谁的手机存谁的。
- 分享靠 URL：整份计划用 base64 编码塞进 `?s=`，对方点开就是完整的一天，**服务器不存任何东西**。
- 唯一的外部请求是天气（open-meteo，无需 key），拿不到就静默回落到手动选。

这套形态的好处是零成本、零维护、断网也能用、没有隐私问题。**接 API 不是为了替换它，而是在它旁边加一层可选的增强：接口挂了、超时了、没配，网站要和现在一模一样能用。**

---

## 三、接 API 的接入点：`Sources`

`index.html` 里第 3 节 `Sources` 就是为此留的那一层。它现在长这样：

```js
Sources.cards(ctx, cb)          // 卡池：默认本地 POOL
Sources.weather(lat, lon, cb)   // 天气：默认 open-meteo
Sources.sweet(ctx, cb)          // 情话：默认本地 SWEETS
```

三个都是「回调 + 失败静默回落本地」。加 `?api=https://你的接口域名` 就会走远端，不加就完全离线。
这样可以先拿一个测试后端跑 `?api=...`，验证完再决定要不要设成默认。

### 约定的返回格式

**`GET /cards?lat&lon&hour&budget&distance`**

```json
{ "cards": [
  { "category": "食", "title": "临江那家小馆", "detail": "点一道她没吃过的招牌菜",
    "weather": "皆", "distance": ["🌆 市区逛逛"], "budget": 1,
    "window": [11, 19], "minutes": 80, "source": "amap:B0FF..." }
]}
```

| 字段 | 含义 | 缺省 |
|---|---|---|
| `category` | `行` / `游` / `食` / `彩蛋` | `游` |
| `weather` | `皆` / `晴` / `雨` | `皆` |
| `distance` | 允许的距离档，数组 | 市区 |
| `budget` | 0 穷开心 / 1 小奢侈 / 2 豪华版 | 0 |
| `window` | `[最早开始, 最晚开始]`（整点小时） | 不限 |
| `minutes` | 大概要多久，排时间轴用 | 玩 90 / 吃 75 / 彩蛋 30 |

远端卡片会**追加**在本地卡池后面一起抽，不会把手写的那些文案挤掉——文案是这个网站的命，不能交给接口。

**`GET /weather?lat&lon`** → `{ "temp": 26, "code": 3 }`（`code` 用 WMO weather code，和 open-meteo 一致）

**`GET /sweet?hour`** → `{ "line": "……" }`

---

## 四、按优先级排的三步

### 第一步：让卡片变成"真的地方"（收益最大）

现在卡片写的是"老字号小馆"，用户还得自己想是哪家。接 POI 之后可以变成"**城南那家 · 步行 8 分钟 · 今天营业到 21:30**"，计划立刻从"一个想法"变成"一个能出门的安排"。

- 数据源：高德/腾讯地图 Web 服务（国内首选，有周边搜索 + 营业时间）、Google Places（海外）。
- 卡池怎么合：保留本地卡片的**文案和玩法**，只把"去哪"替换成附近真实地点——即卡片是模板，POI 是填空。
- **必须有一层代理**：地图 API 的 key 不能放进静态页面（会被人拿去刷额度）。最省事的是一个 Cloudflare Worker / Vercel Function，做三件事：藏 key、按经纬度网格缓存结果、把返回裁成上面那个 `cards` 格式。
- 顺带解决：营业时间比现在手写的 `window` 准得多，时间轴会更靠谱。

### 第二步：短链 + "她拆开了"

现在的分享链接有 800～1200 个字符，能用，但发到聊天窗口里很长，也没法做链接预览图。

- `POST /plans` 存一份计划 → 返回 6 位 id；`GET /p/:id` 取回。链接变成 `…/?p=aB3x9Q`。
- 有了服务端渲染的机会，就能给链接配 `og:image`（一张计划封面），微信/iMessage 里会显示成一张卡片，点开率完全不同。
- 顺手能做**「她拆开了」**：`GET /p/:id` 第一次被打开时给发起人推一条提醒。这是这类产品里最打动人的一个功能，成本却很低。
- 注意：一旦开始存计划，就有了数据责任。建议**不要账号**，只存计划本身 + 30 天过期，链接即凭证。

### 第三步：让文案会写字

- 用 LLM 根据「今天的天气 / 时间 / 已选的几站 / 称呼」现写一句话，替换固定的 `ENDINGS` 和情话。
- 更值钱的用法不是情话，而是**给每一站写一句只属于他们的备注**（"上次你说想吃这家的糖水，今天补上"）。这需要一点历史数据（回忆本），所以排在短链之后。
- 同样要走自己的代理，key 不能进前端；要设每日额度上限，并且失败一定回落到本地文案。

---

## 五、明确不做的方向

- **登录 / 账号体系**：会把 2 步变成 6 步。回忆本用 `localStorage`，需要跨设备时用「设置链接」搬过去就够了。
- **社交流 / 广场 / 别人的约会计划**：这是一个两个人的东西，第三个人一进来就不心动了。
- **打包成 App**：分享路径是链接，装 App 只会让链接更难点开。
- **付费墙**：如果真要收钱，收的应该是"帮你订好"（第一步之后的代订/日历导出），不是"解锁更多卡片"。

---

## 六、维护须知

- 全站就一个 `index.html`，JS 按 12 个编号小节排好，改动前先看小节标题。
- 有意只用 ES5 语法（`var` / `function` / 回调），因为很多人是在微信内置浏览器里打开的，老 webview 上不能崩。
- 兼容性别丢：旧的 `?c=` 设置链接必须一直能打开，已经发给别人的链接不能失效。
- `favicon`、彩蛋（连点 5 次页脚）、`4-15` 和 `12-6` 的自动彩蛋都是有意保留的，别顺手删掉。
