name = "xindong"
main = "worker.js"
compatibility_date = "2026-01-01"

# 先执行 `wrangler d1 create xindong`，把它返回的 database_id 填到下面
[[d1_databases]]
binding = "DB"
database_name = "xindong"
database_id = "把 wrangler d1 create 返回的 id 填在这里"
